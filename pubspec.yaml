name: hello_flutter
description: "POS Tienda a Tienda — facturación e inventario para ventas de campo."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.0
  intl: ^0.20.2
  provider: ^6.1.5
  flutter_dotenv: ^6.0.1
  dio: ^5.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/zoe_logo.png
    - .env
